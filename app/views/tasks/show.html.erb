<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><input type="checkbox" <% "checked" if @task.completed %>>
<%= @task.completed ? "This task is completed": "This task is not completed" %></p>
<%= link_to 'Back to list', index_path %>
<%= link_to 'Edit task', edit_task_path %>
